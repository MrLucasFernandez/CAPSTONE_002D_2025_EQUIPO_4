import React from 'react';

const App: React.FC = () => {
    
    return (
        
        <React.Fragment>
            {/* El contenido específico de la ruta activa se renderiza automáticamente */}
        </React.Fragment>
    );
};

export default App;