<!doctype html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Confirmación de compra</title>
        <style>
            body{font-family:Arial,Helvetica,sans-serif;margin:20px;}
            h2,h3{color:#333;}
            table{width:100%;border-collapse:collapse;margin-top:10px;}
            thead tr{background-color:#f2f2f2;}
            th,td{border:1px solid #ddd;padding:8px;}
            th{text-align:left;}
            .totales{margin-top:15px;font-size:14px;}
        </style>
    </head>
    <body>
        <h2>¡Gracias por tu compra, {{nombreCliente}}!</h2>
        <p>Hemos recibido tu pedido con número <strong>#{{idBoleta}}</strong>.</p>

        <h3>Detalle de productos:</h3>
        <table>
            <thead>
                <tr>
                    <th>Producto</th>
                    <th>Cantidad</th>
                    <th>Precio Unitario</th>
                </tr>
            </thead>
            <tbody>
                {{#each productos}}
                <tr>
                    <td>{{this.nombre}}</td>
                    <td>{{this.cantidad}}</td>
                    <td>${{this.precioUnitario}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>

        <div class="totales">
            <p><strong>Total:</strong> ${{totalBoleta}}</p>
            <p><strong>Impuesto (19%):</strong> ${{impuesto}}</p>
            <p><strong>Compra realizada el día:</strong> {{fechaFormateada}}</p>
        </div>

        <p>¡Muchas gracias por tu preferencia!</p>
        <p>Si tienes algún inconveniente, contáctanos mediante nuestro sitio web.</p>
    </body>
</html>
